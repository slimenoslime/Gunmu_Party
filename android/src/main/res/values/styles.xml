<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      全屏无标题栏主题。游戏自己负责画所有 UI，
      因此状态栏与标题栏都交给系统隐藏，交给 libGDX 的 hideStatusBar 保持。
    -->
    <style name="GdxTheme" parent="@android:style/Theme.Black.NoTitleBar.Fullscreen">
        <item name="android:windowBackground">@color/gunmu_window_bg</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
