# 06 · 决策表与未决项

> 上位文档：`00-OVERVIEW.md`。
> 🔒 = 用户拍板（不可改） / 🧭 = 代拍决策（可被用户否决） / ❓ = 未决项

---

## 1. 决策表

| 编号 | 决策 | 记号 | 依据 / 理由 | 复核触发条件 |
|---|---|---|---|---|
| **D1** | 引擎 LibGDX 1.13.5，Java 21，`core` + `lwjgl3` 双模块 | 🔒 | 用户指定 LibGDX | 需上移动端时加模块，不改本项 |
| **D2** | 物理自研（heightfield + 等效球 + OBB），不用 Bullet | 🧭 | 确定性 + 零原生依赖 + 为联机输入回放铺路 | 出现自研物理无法表达的需求（如真圆柱啮合） |
| **D3** | 渲染为程序化 3D，零外部美术文件，但预留 `assets/` 覆盖点 | 🧭 | 用户明确禁止擅自生成占位美术 | 用户提供正式美术资源时切换 |
| **D4** | 晋级一律为**名额制（quota）** | 🔒 | 用户口述"前 N 名晋级"，一般化为硬上限 | 不可改 |
| **D5** | 三模式 RACE / SURVIVAL / ARENA，模式决定"碎掉之后发生什么" | 🧭 | 用户口述的"竞速/生存/竞技"三类 | 出现第四类玩法 |
| **D6** | **「碎掉→重生到存档点」只适用于 RACE 图**；SURVIVAL 不重生、ARENA 按关区分 | 🔒 | 收窄旧规则适用域，解决"死亡满 5 人晋级"与"无限重生"的冲突 | 不可改（除非用户改生存规则） |
| ~~**D7**~~ | ~~L3 两阶段：竞速段锁 10 名额 + 生存段补足至 15~~ | ❌ **已废止** | 见 D57：原判断是「L3 = 竞速段 + 生存段连跑」，玩家澄清其实是**二选一抽一张图** | — |
| ~~**D8**~~ | ~~L4 三子图连战（竞速→生存→竞技），子图间不淘汰、满血重置~~ | ❌ **已废止** | 见 D57：同 D7，玩家澄清是**三选一抽一张图** | — |
| **D9** | L5 禁复活，唯一冠军；超时按血量→人头→伤害排序 | 🧭 | 用户口述"被击杀无法复活" | 用户对超时规则有异议 |
| **D10** | 撞击伤害单向判定：`clamp((vRel−8)×2.0, 0, 45)`，攻击方吃 15% 反伤 | 🧭 | 用户"高速撞到别人会把他撞受伤"的具体化 | 实战手感偏差时调系数 |
| **D11** | 道具稀有度由权重决定（≥10 普通 / 6-9 稀有 / 5 史诗），同点连续 3 次普通则保底稀有 | 🧭 | 避免刷新点运气锁死 | 用户觉得保底规则多余 |
| **D12** | 伤害反弹**递归上限 1** | 🧭 | 否则两个外耗玩家互撞会无限递归 | 不可放宽 |
| **D13** | 偷来的决战技**优先于**自身技能施放 | 🧭 | 只有 1 次，不用即浪费；且避免 CD 中误按无反馈 | 用户希望手动切换 |
| **D14** | 正太扭腰：**免伤 + 免控，但不免推挤**（会被撞开） | 🧭 | 否则 3 秒站桩无解，破坏 L5 决战节奏 | 实战中过强/过弱时调 |
| **D15** | 中国人能飞的 25 点伤害为**真伤**（不吃加成也不被减伤） | 🧭 | 否则与刀盾/破防叠加后数值失控 | 用户认为应按普通伤害结算 |
| **D16** | 忘情牛肉面：受伤**或**主动攻击都提前解除隐身 | 🧭 | 否则隐身可无代价贴脸输出 | 用户希望只有受伤才解除 |
| **D17** | 共 10 张地图（RACE 4 / SURVIVAL 3 / ARENA 3），同模式地图必须满足公平性基线 | 🔒 | 用户口述"同一种模式也有多张地图，机关路线各不相同" | 不可改（数量可增） |
| **D18** | 道具 23 个，全部取自 2026 热梗；刷新点位置固定，重刷默认 12 s（L5 = 9 s） | 🔒 | 用户口述"刷新点固定""与最近 2026 热梗相关" | 不可改 |
| **D19** | 决战技仅 L4/L5 可用；进入 L4 前选，进入 L5 前可重选一次 | 🔒 | 用户口述只在第四关与决战关提"可以选择决战技" | 用户希望 L1-L3 也能用 |
| **D20** | AI 与真人**共用同一个 `RollInput` 接口** | 🧭 | 保证 AI 不作弊，也让后续联机的服务端接管零成本 | 不可放宽 |
| **D21** | 所有实体遍历按 **ID 升序**，禁用哈希迭代顺序 | 🧭 | 确定性硬要求（D2 的推论） | 不可放宽 |
| **D22** | 地图数据在注册时跑硬校验，失败**直接抛异常** | 🧭 | 禁止静默降级（用户明确反对以"简化"名义砍东西） | 不可放宽 |
| **D23** | 每根滚木同时只带 1 个道具；背包满踩到道具不消失、玩家被弹开 | 🧭 | 避免"道具被踩没"的挫败感 | 用户希望允许叠放 |
| **D24** | 决战技 CD 在"施法中被碎"时返还 50% | 🧭 | 惩罚但不至于让人一场用不出第二次 | 用户希望全额/不返还 |
| **D25** | 平地限速 9 m/s，下坡上限 22 m/s，狂暴态撞击 ×1.3、转向 −40% | 🧭 | 用户口述"下坡可以快速滚动"的数值化 | 手感调整 |
| **D26** | RACE 走廊统一放宽：`halfWidth ×= 1.4`、`shoulder += 2.5` | 🧭 | 冒烟测试实测 **157 次死亡里 148 次是"坠落深渊"**——初版窄道让整局变成比谁少掉下去，而不是比谁滚得快。放宽后死亡降到个位数，且 25/25 全部到线 | 手感偏宽时收紧 `MapRegistry.ROUTE_WIDTH_SCALE` |
| **D27** | AI 增加**走廊保持**：按横向偏移比例把目标点收回到当前中心线，同时按偏移量松油门、压制抖动；给油量改为随速度缩放（`12 + v·1.4`） | 🧭 | D26 的同批实测：AI 在冰面图（friction 0.35）上无法维持走线。加入走廊保持后，冰隙峡谷 0 死亡、全员到线 | AI 变得太强/太弱时调 `BotBrain.thinkRace` 的 `off` 阈值与 `steerStrength` 下限 |
| **D28** | 桌面与安卓**共用同一份 `core`**，差异由一个 `mobile` 开关收敛（见 `05` §2.1） | 🔒 | 用户要求"同时支持 Windows 和 Android"。双份代码必然漂移，因此只允许一个开关 | 不可改 |
| **D29** | 渲染层**不使用 `ModelBuilder` / `ModelInstance`**，几何直接产出 `MeshPart + Material`，塞进池化的 `Renderable` | 🧭 | 当前 libGDX 版本 `Model` 只能由 `ModelData/ModelBuilder` 构造、`ModelInstance.model` 与 `Renderable.meshPart` 都是 `final`，导致"同几何多颜色"只能复制网格；本作实体量大，复制网格内存直接翻数倍 | 上游 API 放开后再考虑换回 |
| **D30** | 中文字体**随包分发**：`tools/make_font.py` 扫描全部 `.java` 源码取出用到的字符，用 `pyftsubset` 从 Noto Sans SC（OFL）裁出 `assets/fonts/game.ttf` + `assets/fonts/charset.txt`；运行时的字形烘焙集直接读 `charset.txt` | 🧭 | 桌面能借系统字体，安卓各机型的 `/system/fonts/*` 路径与可读性都不统一；而整份 Noto Sans SC 有 8 MB。裁剪后 **260 KB / 1262 字形**，两端渲染完全一致，也不再有"字体没找到就退化成 ASCII"的隐性风险 | 换字体或加入大段新文案时重跑 `tools/make_font.py` |
| **D31** | 构建链提供**不依赖 Gradle 的降级路径**：`tools/fetch_libs.py` 抓依赖、`tools/fetch_natives.py` 解 `.so`、`tools/build_jar.py` 出桌面 jar、`tools/build_apk.py` 出 APK | 🧭 | 受限环境里 Gradle 的 `native-platform` 会因创建 `.lock` 失败而**连启动都进不去**（`Could not initialize native services`），但它拦的是"调度层"，不是"编译本身"。APK 的构造只是 `aapt2 compile → link → javac → d8 → 打包 → zipalign → apksigner` 七步机械操作，直连工具链没有守护进程与缓存状态，产物可复现 | Gradle 恢复正常后，两套并存；以 Gradle 为主，脚本作为可复现的对照 |
| **D32** | 依赖清单**必须从构件元数据推导**，不许凭记忆手抄 | 🔒 | libGDX 1.13.x 把 `SharedLibraryLoader` 从 `gdx.jar` 拆到独立构件 `com.badlogicgames.gdx:gdx-jnigen-loader`。`GdxNativesLoader` **编译期不引用、运行期才引用**它 —— 于是 `javac` 一切正常、`gradle build` 也过，游戏一启动就 `NoClassDefFoundError: com/badlogic/gdx/utils/SharedLibraryLoader`。手工抄的 jar 清单必然漏。清单现落在 `tools/fetch_libs.py`，并附 `REQUIRED` 关键类校验（`--verify`） | 升级 libGDX 版本时重跑 `tools/fetch_libs.py --verify` |

| **D33** | 相机改为**绕看点环绕**：位置 = 看点 − 朝向×水平距离 + 上方高度，`direction` 由「看点 − 相机位置」反算；看点再沿赛道前瞻 7m、抬高 1.6m。输入从**屏幕空间**按相机 yaw 旋转到世界空间 | 🔒 | 旧版有三个错：①`CAMERA_HEIGHT_ABOVE` 与 `CAMERA_DISTANCE` 叠加，屏幕中心落到玩家头顶上方 9m，玩家被顶出画面下沿；②`direction` 写死成 `(0,sin,cos)`，与相机实际位置不匹配，**相机根本没看向玩家**；③输入直接当世界方向用，W 键在往镜头方向退。三个都是"看着就不对"但断言抓不到的 | 手感调整只动 `CAMERA_PITCH` / `CAMERA_DISTANCE` / `CAMERA_LOOKAHEAD` |
| **D34** | 地形网格必须**分块**（96 格一块）且三角形**绕序朝上**；`MeshFactory.collectTerrainTile` 抽成纯计算，供无头审计直接调用 | 🔒 | ①`Mesh.setIndices` 只吃 `short[]`，单网格顶点上限 65535，冰隙峡谷的 ICE 材质组有 67960 个顶点，索引回绕把地形撕成碎片；②原绕序 `0→1→2 / 0→2→3` 算出的几何法线是 **−Y**，而顶点法线是 +Y，开启背面剔除后**整片地面被剔掉** —— 这才是"地面看不见"的真因，截图实测清屏色占比 83.4%，修复后 41.8% | 换 `TERRAIN_TILE` 只影响块数与 draw call |
| **D35** | `box()` 的顶点位置必须写成 `(面法线 + u·su + v·sv) × h` | 🔒 | 原来漏掉沿面法线的那一项，六个面全部落在过原点的平面上 —— 得到的不是立方体而是六张互相穿插的纸片，从背面看还全被剔除。墙体/机关/护盾全是这个几何 | 不可改 |
| **D36** | 渲染问题必须有**可断言的检查**：`PrimitiveTest`（逐面查几何法线是否朝外 + 包围盒尺寸）、`TerrainAudit`（每张图统计朝上三角形占比）、`check_shot.py`（解 PNG 统计清屏色占比） | 🔒 | 这一版带着"地面整片消失"交付过，而编译、地图校验、整局冒烟**全部通过**。断言抓不到的问题，就会一路走到用户面前 | 新增任何程序化几何都要进 `PrimitiveTest` |

| **D37** | **调试开关不得把玩家锁死**：`-Dgunmu.autoplay` 打开时必须有 HUD 明示 + `F2` 开关 + 「玩家一动方向键/摇杆就立刻交还控制权」三重出口；进关日志必须写明当前控制方式 | 🔒 | 把 `-Dgunmu.autoplay=true` 的抓帧命令直接写进了文档代码块，用户照抄之后**控制不了自己的滚木、也找不到开关**。调试后门必须默认无害、可发现、可退出，否则它就是一个面向用户的 bug | 新增任何调试开关都要照这三条做 |

| **D38** | 屏幕空间 → 世界空间的输入映射抽成纯函数 `core/InputMapping`，并由 `InputMappingTest` 拿 `PerspectiveCamera` 的**真实视图矩阵基向量**逐朝向验证 | 🔒 | 横向两项符号写反 → **按 A 往右、按 D 往左**。推导：`setToLookAt` 里 `屏幕右 = up × (−朝向)`，yaw=0 时等于 `(−1,0,0)`，而不是 `(+1,0,0)`。这种错编译通过、物理冒烟全绿，只有上手玩才发现；断言用相机自己的基向量做参照，符号再改错会立刻红（实测 D→右 点积会变成 −1.0000） | 改 `InputMapping` 的任何符号都要重跑 `tools/run_smoke.py` |

| **D39** | 随机种子交给 `Random` 前先做 **murmur3 雪崩混合**（`LevelDirector.mixSeed`）；一局之内**赛道图不重复**；`-Dgunmu.seed` 只允许用于复现问题 | 🔒 | 两条都是玩家报的"每关地图都是固定的"。①`new Random(seed)` 只是常数异或再线性同余，**相邻种子的高位几乎一样**，而 `nextInt(4)` 取的正是高位 —— 实测种子 1..6 的第一次 `nextInt(4)` **全都是 2**，48 个种子抽到的是同一张图；②我以前把 `-Dgunmu.seed=20260921` 写进了 README 的"怎么跑"里，固定种子 = 每局完全一样 | 换 RNG 实现时重跑 `MapPickTest` |
| **D40** | 存档点判定改为**按赛程弧长**（越过 `Checkpoint.arc` 就存档），画面也画成**横跨赛道的带状区域** | 🔒 | 旧做法是在固定位置放一个半径 2.5m 的触发圆盘：被撞飞、走边线、高速掠过都可能从旁边擦过去，结果是"我明明过了这一段却没存上"。弧长判定不可能漏，画面表达也和规则一致 | 不可改 |
| **D41** | 机关与区域贴地必须**按地形法线倾斜**：`setToTranslation(p).rotate(slopeQ).rotate(轴, 角度).scale(...)` | 🔒 | 机关原来只绕 Y 轴摆，在斜坡上一转就一头扎进地里、一头悬空（玩家报的"机关转动时穿模"）。`Matrix4.rotate` 是右乘，所以 slopeQ 写在最前 = 最后作用于顶点，自转轴自动变成坡面法线 | 换渲染顺序时注意此约束 |
| **D42** | 地面标记一律用**空心圆环** `KIND_RING`，不用实心圆盘 | 🔒 | 玩家报"玩家下面有一块无碰撞的草的圆形地板"。查明是**玩家高亮标记**：琥珀色 (255,183,3) 的实心圆盘半透明叠在冰面上会变成淡黄绿色，看起来就是一块草地/平台。环只画轮廓，一眼能看出是标记 | 新的地面标记也照此办理 |
| **D43** | **每条输入都必须在物理/系统层有唯一的消费点，并且读到就清**：`jump` → `PhysicsWorld.stepOne`、`useItem` → `ItemSystem`、`useUltimate` → `UltimateSystem`。新增任何 `RollInput` 字段都必须同时给出消费点 | 🔒 | 玩家报"怎么还是没法跳跃"。查明 `input.jump` 只有**两处写入**（`Hud`、`BotBrain`）、**全仓零处读取**，`GameConfig.JUMP_VELOCITY` 定义了也从没被用过 —— 跳跃整条链路是断的，而编译、地图校验、整局冒烟**全部通过**。`RollInput.clearEdges()` 也同样从未被调用过 | 新增输入字段时同步 `JumpTest` 式的端到端断言 |
| **D44** | 贴地判定：**上升中（`vel.y > 0`）绝不贴地** | 🔒 | 起跳当帧的上升位移可能小于 `resolveGround` 的 0.06 贴合余量，于是被同一步按回地面，表现为"按了空格没反应"。用 `vel.y` 判定而不是计时器，是因为站立、上下坡、贴地滚动时 `vel.y` 恒为 0 或负 | 不可改 |
| **D45** | 沿路径的进度必须**同时按横向与高度**判定，唯一实现是 `map/TrackProgress`（`LevelDirector` 与无头审计 `RouteAudit` 共用同一份）；读不到弧长时**冻结上一次值**。另新增 `Route.nearestTrack`（含高度的最近点），而 `Route.nearest` 本身**不许改** | 🔒 | 玩家报"之字断崖跳到虚空里能直接读到下面的存档点和通关区域，因为销毁不是瞬间销毁的"。进度原先是无条件 `route.nearest(x, z)` —— **只看水平坐标**，折返图上下两段路在俯视图上靠得近，掉进虚空时最近点跳到更靠后的一段，弧长瞬间跳几百米，而坠落判定要等掉够几米才生效。审计实测：**之字断崖 2021 个虚空格子能偷进度、最多偷 356.1m**（全图 620m）。地形也是用同一个 `nearest` 裁的，所以连几何本身都带这个歧义；用它做最近点查询才是两件事口径一致 | 改 `TrackProgress` 的判据必须重跑 `RouteAudit`（含"所有可行走格必须可读"的反向检查） |

| **D46** | 开局 **8 秒起点屏障**：所有人被关在起点区域内、可以自由移动、跳不出去；屏障期间**比赛时钟与生存关危险区都不推进**；起点区域由出生点推导（形心 + 最远出生点 + 3.5m，下限 11m） | 🔒 | 需求来自用户口述。三条实现选择都有理由：①屏障**不限高**——按高度判定会被"起跳+被弹飞"越狱；②只抹朝外的速度分量而不是弹开——弹开会把人反复推来推去，"可以随便移动"就没了；③起点区域**必须推导**，三种模式的出生点布局（方阵 / 环形）完全不同，手写必然漂移 | 时长改 `START_BARRIER_TIME`；半径余量改 `START_BARRIER_MARGIN` |
| **D47** | 道具卡必须**显示完整属性**（名称/稀有度/作用对象/持续/完整说明/梗出处）；中文一律**按字符折行**，不用 `GlyphLayout` 的自动折行 | 🔒 | 玩家报"道具属性的显示范围太小了，应该要显示完整属性"。旧实现是一行 `道具 名字 — 说明` 且不折行，中文长句被屏幕右边缘（甚至被状态面板边框）切掉。`GlyphLayout` 的自动折行**按空格断词**，中文整句没有空格 → 会被当成一个超长单词原样溢出，必须自己按字折。折行算法抽成纯函数 `Fonts.wrapText` 以便无头断言"无损 + 不溢出" | 新增界面文案后重跑 `tools/make_font.py`（D30） |

| **D48** | **加速**：`Shift` / 触屏「加速」，移速 **×1.5**、持续 **3 s**、冷却 **15 s**；作用点是 `Roll.speedMultiplier()`（上限+加速度一起放大，不是瞬时冲量）；**重生只清生效中的加速、不清冷却** | 🔒 | 需求来自用户口述。`Shift` 是**边沿**输入，与跳跃同一条规矩（D43）：唯一消费点 `PhysicsWorld.stepOne`，读到即清。冷却属"资源"不属"状态"，与 `ultimateCd` 一致 | 数值改 `GameConfig.DASH_SPEED_MULT / DASH_TIME / DASH_COOLDOWN`；改完重跑 `tools/DashTest` |
| **D49** | **道具刷新池按模式过滤**：竞速图剔除"战斗专用"道具（`ItemType.combatOnly()`，6/23），可刷池 17 个。过滤只写在 `MapDef.weightOf()` 一处，**盲盒也走同一口径** | 🔒 | 玩家报"竞速关会刷出防御类的道具，这些只在生存图有用"。竞速图里纯防御/纯输出/反隐都没有适用对象。盲盒是后门，不堵住等于没修 | 新增道具时必须同时决定 `combatOnly()`；`tools/ItemPoolTest` 会断言"竞速图出现集合 == 全集 − 战斗专用" |
| **D50** | 起点屏障生效期间，**圈内所有滚木免伤**（`Roll.startShield` 并入 `invulnerable()`），每帧按"是否在圈内"重算 | 🔒 | 某些竞技图的出生点半径内本来就压着伤害区域（熔岩环），那 8 秒照常结算伤害就会有人在起跑线上被烧死 —— 和操作无关。放在 `invulnerable()` 里是因为那是**唯一**的伤害汇总口径，只改一处就不会漏；每帧重算则是为了保证屏障一消失保护立刻收回 | 不可改 |

| **D51** | `MapDef.route` 是**可空字段**（只有 RACE 图有折线路径）：任何解引用都必须走 `hasRoute()` / `routeLength()`；"按模式有条件成立"的查询全部收进 `map/MapNav`（纯函数）；`tools/run_smoke.py` 增加**源码级检查**，`.route.` 解引用若同行没有守卫也没挂 `// route-ok: 原因` 就直接让构建失败 | 🔒 | 真崩溃：进「涨水盆地」（生存图）时 `LevelScreen.logDiagnostics` 读了 `route.totalLength`，`NullPointerException` 把进程干掉。代码在 Screen 里、**开窗口才跑得到**，单元测试够不着 —— 所以既要把逻辑抽成可测的纯函数，也要有一条能覆盖"将来新写的解引用"的静态护栏。护栏上线即抓出 5 处 | 新增可空字段时照此办理 |
| **D52** | 诊断/界面文案按模式给不同的量（`MapNav.progressText`）：竞速报赛程米数、生存报存活秒数、竞技报击杀 | 🔒 | 非竞速图以前也被写成"进度 x/y"，而生存图的 `progress` 其实是 `存活秒数 × 10`，硬套只会误导排查 | 不可改 |

| **D53** | 起点/终点缓冲台的生成顺序必须是**由远及近依次插到最前面**（`for (i = 1; i <= 3; i++) insert(0, first − u·12i)`） | 🔒 | 原来写的是 `for (i = 3; i >= 1; i--)`，得到的顺序是 `−12 / −24 / −36 / first` —— **路径在起点处凭空折返 32 米**。后果不是"多绕一点路"：折返段与后面的路在空间上完全重叠，`nearest` 在两条弧长之间乱跳（偷鸡），出生点也被摆到折返段上、脚下一会儿是起点高度一会儿是中段高度。实测（青丘缓坡）段长本该 12/12/12，实际是 10.4/10.4/**31.2** | 改 `extendPads` 后必须跑 `RouteOverlapTest` |
| **D54** | **任意两条相隔 2 段以上的路段不许在水平面上相交**（`RouteOverlapTest`）；之字断崖按"每条横向带子之间留 ≥ 90 米 z 间距"重排，终点不再折返回中段 | 🔒 | 地形是"按离最近路段逐格定高"生成的，而**一个格子只能存一个高度**。两条路圆面相交、高度差又大时，高的那条会盖住低的，玩家走在低的那条上会被 `resolveGround` **瞬移到高的那条**。旧之字断崖实测：终点层 (82, 6, −288) 与中段 (76, 112, −292) 水平距 **0.0 米、高度差 114 米**；起点 (0, 150, −280) 也压在中段头上，出生点直接落进中段地形（出生点 y=123 而起点路面 150）。这正是玩家报的「一到终点就被瞬移到上面、而且还能偷鸡」 | 改地图坐标先跑 `tools/quick_route.py` |
| **D55** | 进度判定加**段滞后**（`TrackProgress.arcAt(..., hint)`，优先沿用上一帧的段及其邻居），且**弧长按未钳制的 t 外推** | 🔒 | 两条独立的事：①弯道处两条边的距离几乎相等，纯全局最近点搜索会在两者之间来回切，弧长跟着一帧跳十几米（而滚木一帧最多走 0.37m）；②曾经把 `t` 钳到 `[0,1]` 之后拿它算弧长，于是玩家走到段末之外时弧长被卡住、越界后再一次性补回 `0.25 × 段长 ≈ 9 米`。修完，之字断崖的冻结帧从 **964 → 9**、跳变从 **382m → 22m**，4 张图全部能在 55~65 秒跑完（时限 300 秒） | 改 `TrackProgress` 后重跑 `RaceRunTest` 与 `RouteAudit` |
| **D56** | AI 的 `steer()` **接近目标要松油门**（按 `v²/MOVE_ACCEL` 估刹车距离，进入该半径后按距离比例收推力） | 🔒 | 玩家报「所有 AI 都一直在前后乱动」。滚木有惯性（摩擦约 1.0/s，9 m/s 刹车要 9 米），一直全油门冲过去的话到了目标停不下来 —— 冲过头、目标落到身后、再掉头冲回来，就是"前后乱动" | 调 AI 手感改这一处 |

| **D57** | 🔒 **第 3 / 4 关是「每关抽一张图」**（L3 从竞速/生存二选一，L4 从竞速/生存/竞技三选一），**不是子图连战**；随之删掉「竞速段锁名额」（`L3_RACE_LOCK_CAP`）、子图积分的语义、以及 `stageIndex` 分段逻辑 | 🔒 | 玩家澄清：「第三关有竞速也有生存，第四关有竞速也有生存也有竞技图，**是三选一啊**，而不是子图」。原实现是 L3 竞速段+生存段连跑、L4 三子图连战 —— 早期理解错设计的产物（D7 / D8 已废止）。改成抽签后，结束条件与名额都回到「每关一张图 + 名额制」这条主线，一并修掉了「关卡莫名其妙就结束/胜利」那类现象 | 新增关卡按「一关一张图」；`SmokeTest` 断言每关只被进入一次 |
| **D58** | 起点屏障**只在 `phase == PLAYING` 时生效** | 🔒 | 结算 / 选技 / 冠军界面也会 `renderScene()`，而换关时 `stageClock` 刚被清零 —— 那三个界面上会立着一个巨大的青色圆筒正对镜头，**把结算卡整个挡住**（玩家报的「晋级图上面有一个大大的东西遮挡住视野」）。屏障是「比赛开始前 8 秒」的东西，比赛结束就不该出现 | 新增「只该在某阶段出现」的东西都用 phase 守 |
| **D59** | 皓宸的偷技判定放宽到**前方 60° 锥形、2~20 米**，兜底范围与它一致 | 🔒 | 玩家报「皓宸在人机局偷不到任何技能」。原来是 30° 锥形、3~12 米，而兜底也是 12 米（等于没有兜底）—— 人机局里 AI 会绕着你走，要恰好罩进这么窄的锥形基本靠运气 | 数值在 `GameConfig.HAOCHEN_*` |

| **D60** | 新增**普通攻击**（`J` / 触屏「攻击」）：5 s 冷却、以朝向为中线的 **70° 扇形 / 5 m 半径 / 15 伤**，**只在第 5 关（决赛）可用**；同时给原本没有攻击能力的 6 个决战技各补一段 8~12 点的小剂量附加伤害 | 🔒 | 玩家口述：「大多数技能都没有攻击能力，首先增加；其次给竞速图多加点实用的速度道具；其次添加攻击技能，5 秒冷却对扇形内的敌人造成 15 点伤害，只在决赛关可用」。8 个决战技里原本只有 2 个能打人，其余放完等于什么都没发生；而"5 秒冷却"这个档位明显不是决战技（决战技是 25~40 s 的大招），所以把它做成**独立的基础输出**而不是第 9 个决战技。只在决赛关开放：前四关要么各跑各的、要么靠地形分胜负，加 5 秒档近身攻击只会让手感变杂 | 数值在 `GameConfig.ATTACK_*`；判定由 `tools/AttackTest` 端到端守着（含"第 4 关按了无效、输入仍被清掉"的反向断言） |
| **D61** | 新增 4 个**竞速向速度道具**（道具 23 → 27）：遥遥领先（上限 22→28）、早八（起步冲量 14 m/s + 加速度 ×1.5）、闪现（向前瞬移 ≤12 m，逐 0.5 m 探路）、一路绿灯（身前铺 33.4×10 m 加速带）；四张竞速图**各偏置一个**（×2） | 🔒 | 同一批口述：「给竞速图多加点实用的速度道具」。竞速图过滤掉 6 个战斗专用道具后剩 17 个，而真正加速自己的只有 3 个 —— 四张图轮着抽很快就腻。四个新道具**都不算 combatOnly**（不限模式），并各自解决一种处境：抬上限 / 抬起步 / 保命位移 / 自己铺路 | 权重与 `combatOnly()` 由 `tools/ItemPoolTest` 穷举兜底（"竞速池 == 全集 − 战斗专用"必须集合完全相等） |

| **D62** | 打包阶段必须有 **dex 类完整性校验**（`tools/verify_dex.py`）：dex 里被引用的每个类，必须是"dex 内定义 / 框架提供 / java 运行时提供"三者之一；**校验不过直接中止构建、不产包**。守卫是"启动必需类"（`ViewCompat`、`OnApplyWindowInsetsListener`） | 🔒 | 真事故：安卓版**一打开就闪退** —— `NoClassDefFoundError: Landroidx/core/view/OnApplyWindowInsetsListener`，在 `AndroidApplication.init` 里。原因是 libGDX 1.13.5 的安卓后端运行期依赖 `androidx.core`，而 javac 不需要解析 `init()` 方法体里引用的类 ⇒ **编译通过、dex 生成成功、签名校验通过**，只有真机启动才炸。判据可静态计算（解析 dex 的 `type_ids`/`class_defs` + android.jar 类表做差集），不需要真机，所以没有理由不做 | 新增任何安卓依赖后重跑；豁免项必须逐条写清"什么条件下才会用到" |
| **D63** | 安卓依赖闭包取自 `.module` 的 **`java-runtime` variant**（不是 pom），并设**白名单闸门** + **下载前版本对齐** + 补 `-jvm` 实现变体；库的 **R 类**由 `build_apk.py` 照 AGP 的做法用 `aapt2 --extra-packages` 生成 | 🔒 | ①按 pom 递归会拉出 `lifecycle-compiler`、`lifecycle-livedata-core-ktx-lint`（构件不存在、直接 404）、guava、rxjava（实测 60 个构件 24 MB）；②不设闸门会带出 coroutines 的 debug/rx/javaFx/slf4j 变体与 support-v4；③`annotation`/`collection`/`kotlinx-coroutines-core` 这几个本体**一个 class 都没有**，实现全在同版本 `*-jvm` 里（Gradle 靠 `available-at` 取）；④AGP 3.0 起库的 R 类不再随 AAR 分发，不生成就是 `Landroidx/core/R$id;` 空引用。**对齐必须在下载前做**：受限环境拦批量删除，"下完再筛"会留下重复版本的同一个库，而 d8 遇到重复类是直接报错的 | 白名单只许在 `verify_dex.py` 报出缺类之后增长，每加一行都要能说出"缺的是哪个类" |

| **D64** | 打包必须铺 **jar 里的非 class 文件**（"类路径资源"）：`build_apk.py` 的 `classpath_resources()` 按**黑名单**（除编译期元数据外一律铺）把它们写到 APK 根部；`verify_dex.py` 增加**资源完整性**检查 —— dex 里出现的每个"按路径读文件"的字符串，必须在 APK 根部或 `assets/` 下存在，否则**中止构建** | 🔒 | 真事故：安卓版**一进比赛那一帧就崩** —— `File not found: com/badlogic/gdx/graphics/g3d/shaders/default.vertex.glsl (Classpath)`。libGDX 的 `DefaultShader` 用 `Gdx.files.classpath(...)` 读着色器源码（另有 `lsans-15.fnt/.png` 是 `BitmapFont` 的默认字体）。Gradle 打 APK 会把 jar 资源铺到根部（`mergeJavaResource`），直连工具链的路只编 `.class` 就全丢了。**桌面端看不出来**（fat jar 整包合并），所以这个 bug 只在安卓炸 | 口径用黑名单不用白名单（漏一个新 jar 的新资源就是又一次"只有真机才炸"）；实测 26 个 jar 里真正要铺的只有 8 个文件 49.3 KB |
| **D65** | `.so` 的**压缩方式**必须与清单的 `extractNativeLibs` 自洽：`build_apk.py` 的 `check_native_packaging()` 在打包后立刻断言 —— "压缩进包 + 没写 `extractNativeLibs=\"true\"`" 直接**中止构建**。清单里那句因此是**显式**写的，不靠"缺省即 true" | 🔒 | 同类隐患（本轮排查时发现，尚未爆发）：`.so` 现在是压缩进包的，只有安装阶段解压才可用；一旦谁把 `extractNativeLibs` 改成 false（AGP 现代默认就是"不压缩 + 页对齐"），`System.loadLibrary` 立刻失败，表现是**一点就退**，而且比着色器那次更早、堆栈里连 libGDX 都看不到 | 要改成 AGP 的路子就把 `.so` 用 `ZIP_STORED` 写并靠 `zipalign -p` 页对齐，同时删掉清单里的声明（断言会提示） |

**计数校验：D1..D65 = 65 条 ✓**

---

## 2. 未决项

| 编号 | 未决项 | 当前临时值 | 影响面 | 待用户定 |
|---|---|---|---|---|
| ~~**O1**~~ | ~~L4 三子图积分权重~~ | **已结案** | 第 3 / 4 关已改成「抽一张图」，不存在子图积分 | — |

**计数校验：O1 = 1 条 ✓**

---

## 3. 已解决（结案区）

> 结案项保留定案块，防止下次讨论被当未决项重开。

（v1.0 首版，暂无结案项）

---

## 4. 与用户口述的逐条对照（防漏项）

| 用户口述 | 落到哪里 |
|---|---|
| 30 个滚木、轮盘移动 | 总纲 §2 / `01` §2.2 |
| 可以跳跃 | `01` §2.5 |
| 下坡路快速滚动 | `01` §2.4 |
| 前 25 名到达终点晋级 | 总纲 §2.1 / `02` §2 |
| 高速撞到其他滚木会把他撞受伤 | `01` §3.2 + 总纲 §4.1 |
| 碎掉的滚木重生到上一个存档点 | `01` §4（已按 D6 收窄） |
| 道具刷新点、阻挡/扣血 | `03` 全篇 |
| 每人 100 点血 | 总纲 §4 |
| 第二关前 20 晋级 | `02` §2 |
| 第三关竞速+生存（死亡满 5 人晋级），前 15 | `02` §3 |
| 第四关竞速+生存+竞技（选决战技、限时、人头最多前 10、无限复活） | `02` §4 |
| 决战关（选决战技、被击杀无法复活） | `02` §5 |
| 所有关卡地图都刷新道具、刷新点固定 | `03` §2 |
| 同一模式多张地图，机关路线各不相同 | `02` §6 + 总纲 §6.1 |
| 8 个决战技 | `04` 全篇 |
| 技能要有攻击能力 | `04` §2.1 + 逐条（6 个技能各补 8~12 伤） |
| 决赛关的扇形攻击（5 s CD / 15 伤） | `01` §2.7 + D60 |
| 竞速图要有实用的速度道具 | `03` §4.6（4 个）+ `03` §5（四张竞速图各偏置一个）+ D61 |
| 安卓版不能装了打不开 | `05` §9.2.1/§9.2.2/§10.13.2 + D62/D63（dex 类完整性校验）|
| 安卓版一进比赛就崩（找不到着色器） | `05` §9.2.3/§10.13.3 + D64（类路径资源）+ D65（原生库压缩方式自洽）|
| 道具需与最近 2026 热梗相关 | `03` 全篇（27 个道具均标注出处） |
